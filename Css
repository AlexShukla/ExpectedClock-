  transform: translateX(-50%);
  background: rgba(0, 0, 0, 0.8);
  padding: 1rem 2rem;
  border-radius: 8px;
  font-size: 1.2rem;
  color: white;
  text-align: center;
  z-index: 1000;
}

.clock-face .number.dead {
  color: red;
  opacity: 1;
}
